version: "3.8"

services:
  mcp-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-server
    ports:
      - "8111:8111"
    volumes:
      - .:/app
      - /app/.venv
    networks:
      - backend
    restart: unless-stopped

volumes:
  .venv:

networks:
  backend:
    driver: bridge
